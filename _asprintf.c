#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int conteo(char *a)
{
	int i = 0;

	while (a[i] =! NULL)
	{
		i++;
	}

	return (i);
}
char *_asprintf(char *a, char *b)
{
	int len_a, len_b;
	char *cadena_final = NULL;

	cadena_final = malloc(char *) * (len_a + len_b + 1);

}
